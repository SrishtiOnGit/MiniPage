1. Cloned my repo with "git clone https://github.com/SrishtiOnGit/MiniPage.git"
2. created a folder - client..
   3 inside client by "cd client" I created a vite react app - "npm create vite@latest ."
3. Select React + Javascript + Oxilint/ESlint from the options.
4. remove all the predefined code.
